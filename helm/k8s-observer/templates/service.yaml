apiVersion: v1
kind: Service
metadata:
  name: k8s-observer
spec:
  type: {{ .Values.service.type }}
  selector:
    app: k8s-observer
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080

